name: gunslinger_rush
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  supabase_flutter: ^2.4.0
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  custom_lint: ^0.6.4
  dart_style: ^2.3.6

flutter:
  uses-material-design: true
  assets:
    - assets/images/
